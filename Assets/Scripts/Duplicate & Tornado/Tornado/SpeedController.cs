using UnityEngine;

public class SpeedController : MonoBehaviour
{
    public float Speed = 0f;
    public float MaxSpeed = 20f;
    public float SecToMaxSpeed = 30f;

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
